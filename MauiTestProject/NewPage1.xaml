<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiTestProject.NewPage1"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             Title="NewPage1">
    <!--<Grid RowDefinitions="*,Auto"  toolkit:StateContainer.CurrentState="{Binding CurrentState}">
        <VerticalStackLayout toolkit:StateView.StateKey="Start">
        <toolkit:MediaElement
            x:Name="video"
            Grid.Row="0"
            HeightRequest="300"
            WidthRequest="300"
            ShouldAutoPlay="True"
            Source="embed://ElbowExercise.mp4"
            />
        <Button Grid.Row="1" Text="New Main page" Clicked="Button_Clicked">
        </Button>
        </VerticalStackLayout>
    </Grid>-->
</ContentPage>